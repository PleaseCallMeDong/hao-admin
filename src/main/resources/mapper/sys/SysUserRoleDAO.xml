<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.modules.sys.dao.mysql.SysUserRoleDAO">

    <select id="queryRoleList" resultType="com.example.modules.sys.domain.SysRoleDO">
        select sr.*
        from sys_role sr
        left join sys_user_role sur on sr.role_id = sur.role_id
        where sur.user_id = #{userId} AND sr.status = 0
        <if test="addressInfoId != null">
            AND sr.address_info_id = #{addressInfoId}
        </if>
    </select>

</mapper>